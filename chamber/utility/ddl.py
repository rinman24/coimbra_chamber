"""DDL utility module."""

build_instructions = {
    ('experiments', 'table order'): ('a', 'b', 'c'),
    ('experiments', 'ddl'): dict(
        a='ddl a',
        b='ddl b',
        c='ddl c'
        )
    }
