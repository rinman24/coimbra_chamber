language: python
python:
  - "3.5"
notifications:
  email: false
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - python -m pytest -xv --cov=./ --ignore=tests/data/test_sqldb.py
after_success:
  - bash <(curl -s https://codecov.io/bash)